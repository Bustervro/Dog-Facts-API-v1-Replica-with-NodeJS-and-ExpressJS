# Dog Facts API (Node.js + Express)

This project is a simple Dog Facts API built using Node.js and Express.  
It returns random dog facts through an API endpoint.

## Features
- Returns a list of dog facts
- Allows users to request a specific number of facts using a query parameter
- Handles invalid input with error messages

## How to Run the Project

1. Install Node.js
2. Install Express:
